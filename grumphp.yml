grumphp:
  hooks:
    pre-commit:
      tasks:
        - phpstan
        - pint
        - pest

  tasks:
    phpstan:
      configuration: phpstan.neon
      memory_limit: 256M

    pint:
      config: pint.json

    pest:
